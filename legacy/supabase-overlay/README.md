# Legacy Supabase overlay

These files are retained for historical reference after the project switched
to Neon before application bootstrap. They are not active migrations or
scripts and must not be run against the Neon database.

The active database files are in `db/`, and the active database commands are
defined in the root `package.json`.

